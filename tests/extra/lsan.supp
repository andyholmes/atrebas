# SPDX-License-Identifier: GPL-2.0-or-later
# SPDX-FileCopyrightText: 2022 Andy Holmes

# https://gitlab.freedesktop.org/fontconfig/fontconfig/-/issues/77
leak:libfontconfig.so

# https://gitlab.gnome.org/GNOME/gvfs/-/issues/577
leak:g_daemon_vfs_init

# False positives caused by G_TEST_OPTION_ISOLATE_DIRS
leak:g_set_user_dirs

# This suppression covers false positives generated by g_set_user_dirs(),
# gtk_init() and adw_init(). We treat atrebas_ui_init() as the effective source
# since eg. libadwaita changes these around sometimes.
leak:atrebas_ui_init

# GLib
leak:g_quark_init

